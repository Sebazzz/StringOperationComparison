# String operation benchmark
Benchmark / comparison of various string operations using:

- Unsafe pointers
- String Builder
- Naive string concatenation


Runs the following test cases:

- Generation of random string based on GUID
- Replacement of characters within a string

## Building and running the project
Open a command prompt with MSBuild 14 in your `%PATH%` and invoke `build.bat`.